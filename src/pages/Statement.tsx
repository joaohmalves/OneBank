import { useState } from 'react'; import { purchases } from '../mocks/data'; import { money } from '../utils/format';
const months=['Agosto 2026','Julho 2026','Junho 2026'];
export function Statement(){const [month,setMonth]=useState(months[0]);const rows=purchases.filter(p=>p.month===month);return <><div className="title-row"><div><small>EXTRATO</small><h1>Histórico do cartão</h1></div><select value={month} onChange={e=>setMonth(e.target.value)}>{months.map(x=><option key={x}>{x}</option>)}</select></div><section className="panel statement"><p>Disponível apenas para os últimos 3 meses.</p>{rows.map(p=><div className="transaction" key={p.id}><span className="transaction-icon">{p.merchant[0]}</span><span><b>{p.merchant}</b><small>{p.date} · {p.category}</small></span><strong className={p.type==='payment'||p.type==='refund'||p.type==='credit'?'positive':''}>{p.type==='purchase'?'- ':'+ '}{money(p.amount)}</strong></div>)}</section></>}
