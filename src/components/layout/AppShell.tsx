import { Bell, CreditCard, Home, LogOut, Menu, Moon, ReceiptText, Sun, WalletCards } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom'; import { useState } from 'react'; import { useAuth } from '../../contexts/AuthContext'; import { VoiceWidget } from '../cognigy/VoiceWidget'; import { CognigyWidgetEmbed } from '../cognigy/CognigyWidgetEmbed';
const links=[['/dashboard','Visão geral',Home],['/cartoes','Cartões',CreditCard],['/extrato','Extrato',ReceiptText]] as const;
export function AppShell(){const {logout}=useAuth();const [dark,setDark]=useState(false),[open,setOpen]=useState(false);const useRealWidget=import.meta.env.VITE_COGNIGY_ENABLED==='true';return <div className={dark?'app dark':'app'}><aside className={open?'sidebar open':'sidebar'}><div className="brand"><WalletCards/> OneBank</div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} onClick={()=>setOpen(false)}><Icon size={19}/>{label}</NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/>Sair</button></aside><main><header><button className="icon mobile-menu" onClick={()=>setOpen(!open)}><Menu/></button><div><small>Olá, João</small><h2>Seu banco, do seu jeito.</h2></div><div className="header-actions"><button className="icon" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><button className="icon"><Bell/></button><div className="avatar">JH</div></div></header><section className="content"><Outlet/></section></main>{useRealWidget?<CognigyWidgetEmbed/>:<VoiceWidget/>}</div>}
