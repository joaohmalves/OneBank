import { X } from 'lucide-react'; import { cards } from '../../mocks/data'; import { useBanking } from '../../contexts/BankingContext';
export function ComparisonModal(){const {comparison,closeComparison}=useBanking();if(!comparison)return null;const a=cards.find(x=>x.id===comparison.cardA)!,b=cards.find(x=>x.id===comparison.cardB)!;return <div className="modal-backdrop" onClick={closeComparison}><section className="comparison-modal" onClick={e=>e.stopPropagation()}><button className="modal-close" onClick={closeComparison}><X/></button><small>COMPARE OS BENEFÍCIOS</small><h2>{a.name} <em>vs</em> {b.name}</h2><table><tbody>{Object.keys(a.perks).map(key=><tr key={key}><th>{key}</th><td>{a.perks[key]}</td><td>{b.perks[key]}</td></tr>)}</tbody></table><button className="primary" onClick={closeComparison}>Fechar</button></section></div>}
